<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/urlShortener" method="post">
 <h1>Url Shortener</h1>
    <label for="redirectURL">Enter your original URL</label>
    <input type="text" id="redirectURL" name="url" placeholder="https://example.com">
    <button type="submit">Generate</button>
    <% if(locals.shortId){ %>
   <p>Your short url is <%=`http://localhost:80001/urlShortener/${locals.shortId}` %> </p>
        <%} %>
    </form>

    <% if (locals.urls) {%>
        <table>
            <thead>
                <th>S.N</th>
                <th>Short Id</th>
                <th>Redirect URL</th>
                <th>Clicks</th>
                <th>Created By</th>
            </thead>
            <tbody>
                <% locals.urls.forEach((url,index)=>{ %>
                    <tr>
                        <td><%=index + 1 %></td>
                        <td><%=url.shortId %></td>
                        <td><%=url.redirectURL %></td>
                        <td><%=url.visitHistory.length %></td>
                        <td><%=url.createdBy %></td>
                    </tr>
               <% }) %>
            </tbody>
        </table>
   <% } %>
</body>
</html>